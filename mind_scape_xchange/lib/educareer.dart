import 'package:flutter/material.dart';

class EduCareerHubScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('EduCareer Hub'),
      ),
      body: Center(
        child: Text('EduCareer Hub Content'),
      ),
    );
  }
}
